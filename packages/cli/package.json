{
  "name": "action-cli",
  "version": "v0.1.0",
  "type": "module",
  "bin": "./dist/bin.js",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "prepack": "npm run build",
    "build": "bun build src/bin.ts --outdir dist --target node"
  },
  "dependencies": {
    "@jondotsoy/flags": "^2.0.2"
  }
}